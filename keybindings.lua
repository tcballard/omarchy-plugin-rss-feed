-- Installed explicitly by scripts/keybindings.py. Quattro's plugin installer
-- does not run configuration hooks. Keep this binding visible in Super + K.
o.rebind("SUPER + ALT + N", "RSS Feed", "omarchy-shell shell toggle io.github.tcballard.rss-feed")
